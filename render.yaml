services:
  - type: web
    name: styled-by-jules
    runtime: static
    buildCommand: echo "Static site, no build needed"
    staticPublishPath: .
    pullRequestPreviewsEnabled: true
    repo: https://github.com/MarcusRawlins/styled-by-jules
